<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Perfil de Usuario</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="min-h-screen bg-gray-900 flex items-center justify-center p-4">
    <div
        class="max-w-xl w-full bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl shadow-2xl p-8 border-2 border-red-600 relative overflow-hidden">
        <!-- Efectos de neón -->
        <div class="absolute -left-20 top-1/3 w-48 h-48 bg-red-600/20 blur-3xl -rotate-45 pointer-events-none"></div>
        <div class="absolute -right-20 top-1/3 w-48 h-48 bg-red-600/20 blur-3xl rotate-45 pointer-events-none"></div>

        <h1
            class="text-3xl font-bold mb-8 text-center bg-gradient-to-r from-red-600 to-orange-500 bg-clip-text text-transparent uppercase tracking-wider">
            PERFIL DE USUARIO
        </h1>

        <div class="space-y-6">
            <!-- Información del usuario -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <!-- Foto -->
                <div class="flex items-center justify-center">
                    <img src="/fotos/<%=user.foto%>" class="w-48 h-48 rounded-full border-4 border-red-600 object-cover 
                                hover:scale-105 transition-transform duration-300 shadow-lg">
                </div>

                <!-- Datos -->
                <div class="space-y-4">
                    <div>
                        <p class="text-sm font-medium text-red-500 uppercase tracking-wider">Nombre</p>
                        <p class="text-xl font-bold text-white">
                            <%= user.name %>
                        </p>
                    </div>

                    <div>
                        <p class="text-sm font-medium text-red-500 uppercase tracking-wider">Email</p>
                        <p class="text-lg text-gray-300 break-all">
                            <%= user.email %>
                        </p>
                    </div>
                </div>


            </div>
            <div class="flex justify-center w-full">
                <a href="./index.html" class="rounded-2xl text-white p-4 px-8 bg-red-600 hover:bg-red-800">Ir al
                    inicio<a />
            </div>
        </div>
    </div>
</body>

</html>