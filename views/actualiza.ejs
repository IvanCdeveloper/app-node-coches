<!DOCTYPE html>
<html lang="es">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Actualiza Coche</title>
    <link rel="stylesheet" href="output.css">
</head>

<body class="min-h-screen bg-gray-900 flex items-center justify-center p-4">
    <form id="updateForm"
        class="max-w-2xl w-full bg-gradient-to-br from-gray-800 to-gray-900 rounded-2xl shadow-2xl p-8 border-2 border-red-600 relative overflow-hidden">
        <!-- Efectos de neón -->
        <div class="absolute -left-20 top-1/3 w-48 h-48 bg-red-600/20 blur-3xl -rotate-45 pointer-events-none"></div>
        <div class="absolute -right-20 top-1/3 w-48 h-48 bg-red-600/20 blur-3xl rotate-45 pointer-events-none"></div>

        <h1
            class="text-3xl font-bold mb-8 text-center bg-gradient-to-r from-red-600 to-orange-500 bg-clip-text text-transparent uppercase tracking-wider">
            ACTUALIZA COCHE
        </h1>

        <div class="space-y-6">


            <!-- Formulario de actualización -->
            <div class="grid grid-cols-1 md:grid-cols-2 gap-6 relative z-10">
                <div class="space-y-2">
                    <label for="id" class="block text-sm font-medium text-red-500 uppercase tracking-widest">ID</label>
                    <input name="id" value="<%= coche._id %>" type="text" id="id" class="w-full px-4 py-3 bg-gray-800 border-2 border-gray-700 rounded-lg 
                                  text-white cursor-not-allowed" disabled>
                </div>

                <div class="space-y-2">
                    <label for="marca"
                        class="block text-sm font-medium text-red-500 uppercase tracking-widest">Marca</label>
                    <input name="marca" value="<%= coche.marca %>" type="text" id="marca" class="w-full px-4 py-3 bg-gray-800 border-2 border-gray-700 rounded-lg 
                                  text-white focus:outline-none focus:border-red-600 
                                  focus:ring-2 focus:ring-red-600 transition-all duration-300">
                </div>

                <div class="space-y-2">
                    <label for="color"
                        class="block text-sm font-medium text-red-500 uppercase tracking-widest">Color</label>
                    <input name="color" value="<%= coche.color %>" type="text" id="color" class="w-full px-4 py-3 bg-gray-800 border-2 border-gray-700 rounded-lg 
                                  text-white focus:outline-none focus:border-red-600 
                                  focus:ring-2 focus:ring-red-600 transition-all duration-300">
                </div>
                <div class="space-y-2">
                    <label for="potencia"
                        class="block text-sm font-medium text-red-500 uppercase tracking-widest">Potencia
                        (CV)</label>
                    <input type="number" name="potencia" value="<%= coche.potencia %>" id="potencia" class="w-full px-4 py-3 bg-gray-800 border-2 border-gray-700 rounded-lg 
                                  text-white focus:outline-none focus:border-red-600 
                                  focus:ring-2 focus:ring-red-600 transition-all duration-300">
                </div>
                <div class="space-y-2">
                    <label for="precio" class="block text-sm font-medium text-red-500 uppercase tracking-widest">Precio
                        (€)</label>
                    <input name="precio" value="<%= coche.precio %>" type="number" id="precio" class="w-full px-4 py-3 bg-gray-800 border-2 border-gray-700 rounded-lg 
                                  text-white focus:outline-none focus:border-red-600 
                                  focus:ring-2 focus:ring-red-600 transition-all duration-300">
                </div>

                <div class="flex items-end">
                    <button type="submit" id="btn-insertar" onclick="actualizaCoche()" class="w-full py-3 px-6 bg-gradient-to-r from-red-600 to-orange-600 
                                   text-white font-bold uppercase tracking-wider rounded-lg
                                   hover:from-red-700 hover:to-orange-700 transform hover:scale-105 
                                   transition-all duration-300 shadow-lg hover:shadow-red-600/30">
                        ACTUALIZAR
                    </button>
                </div>
            </div>
        </div>
        <form />

        <script>
            document.getElementById('updateForm').addEventListener('submit', function (event) {
                event.preventDefault();
                const id = document.querySelector('input[name="id"]').value;
                const marca = document.querySelector('input[name="marca"]').value;
                const color = document.querySelector('input[name="color"]').value;
                const potencia = document.querySelector('input[name="potencia"]').value;
                const precio = document.querySelector('input[name="precio"]').value;

                fetch(`/items/${id}`, {
                    method: 'PUT',
                    headers: {
                        'Content-Type': 'application/json'
                    },
                    body: JSON.stringify({ marca, color, potencia, precio })
                })
                    .then(response => {
                        if (response.ok) {
                            window.location.href = '/';
                        } else {
                            alert('Error al actualizar el ordenador');
                        }
                    })
                    .catch(error => {
                        console.error('Error:', error);
                        alert('Error al actualizar el ordenador');
                    });
            });
        </script>
</body>

</html>